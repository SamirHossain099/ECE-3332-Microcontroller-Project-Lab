#If statement syntax: if <condition>:
#                           <statements>
#                     else:
#                           <statements>
# There are no curly brakets, the statements are contained by indentation
#multiconditional statement
# if <condition1> or <condition2>:
# if <condition1> and <condition2>:
# The multiconditional operators are: or, and, not
# elif <condition>:
"""
while <condition>:
    <statements>

for <iteration var> in <collection to loop over>:
    <statements>

for <iteration var> in range(min,max)
    <statements
for <iteration var> in range(len(object))
    <statements
"""
