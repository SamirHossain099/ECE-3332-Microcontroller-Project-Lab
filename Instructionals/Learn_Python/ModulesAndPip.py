"""
Use the . operator to access the elements of the module
Look up the python module index online for many python verified modules and documentation
"""
import useful_tools 
print(useful_tools.roll_dice(10))

"""
The useful_tools module is a locally created module we made 
but how can I access the ones online?
some modules are built into python and you can just import right out of the box

other modules are external libraries
they get stored in the python/lib/site packages

pip is a package manager application that allows you to
install, manage, update, and uninstall python modules

pip comes pre-installed in newer versions of python 3

Syntax in terminal:

pip --version (indicates version of pip and if it installed)
pip install <name of python module>
pip uninstall <name of python module>

"""